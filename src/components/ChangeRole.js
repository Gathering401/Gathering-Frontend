import React from 'react';
import { Button } from 'react-bootstrap';

export default function ChangeRole(props) {
  // const { user, currentRole } = props;

  return (
    <Button>Change Role</Button>
  )
}